[build]
builder = "nixpacks"
buildCommand = "npm ci --include=dev && npx prisma generate && npx prisma migrate deploy && npx tsc && cp -r src/public dist/public && cp -r src/idl dist/idl"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
